# 実装計画

- ドキュメント種別: 実装計画書
- プロジェクト名: cleaning_schedule
- 作成日: 2026-09-07 / 版: 0.1
- 前提: 01_要件定義書.md（v1.1）、02_基本設計書.md（v0.3）、04_開発環境セットアップ手順.md（v0.1）

---

## 1. 方針

- フェーズ単位で「動く状態」を積み上げる。各フェーズ末にローカルで動作確認。
- 早い段階でデプロイし、実データ（Airbnb iCal）でパーサを検証する。
- UI は最小限のスタイルで機能優先。デザイン磨きは最後。
- 各フェーズ完了時に git コミット。

---

## 2. フェーズ一覧

| P | 名称 | 目的 | 主な成果物 | 状態 |
|---|---|---|---|---|
| P0 | 基盤 | 起動する土台 | package.json, wrangler.jsonc, schema.sql, D1, Hono骨組み, レイアウト, /healthz | ✅ 完了 |
| P1 | 認証・登録 | ログインの壁 | /register, /login, /logout, cookie署名, CSRF, authミドルウェア | ✅ 完了 |
| P2 | 物件・テンプレ管理 | 同期の前提 | /admin, 物件CRUD, テンプレCRUD, テンプレ項目編集, ユーザー管理 | ✅ 完了 |
| P3 | iCal 同期 | 予約→清掃生成 | icalパーサ, sync, Cron, sync_log, 手動同期, 実データ検証 | — |
| P4 | 清掃一覧・詳細 | 中核画面 | ダッシュボード, 清掃詳細, start/complete/reopen/note, 臨時清掃 | — |
| P5 | チェックリスト | 作業チェック | テンプレ展開, 項目トグル, エリア別表示, 進捗 | — |
| P6 | 写真エビデンス | 記録 | クライアントリサイズ, アップロード, photo_blob, 配信, 削除, 拡大表示 | — |
| P7 | PWA | ホーム画面追加 | manifest, service worker, アイコン | — |
| P8 | 仕上げ | 運用品質 | 履歴画面, 容量表示, エクスポート, エラーページ, 微調整 | — |

MVP ライン: **P0〜P6**（これで実運用可能）。P7/P8 は運用開始と並行。

---

## 3. フェーズ別タスク

### P0 基盤

- [ ] `npm init` / `type: module` / scripts / `npm i hono`
- [ ] ディレクトリ雛形、`.gitignore`、`public/.gitkeep`
- [ ] `src/db/schema.sql`（02 §3.1 の DDL + 初期データ）
- [ ] `npx wrangler d1 create cleaning_schedule` → `database_id` 取得
- [ ] `wrangler.jsonc` 更新（assets / d1 / cron / vars）
- [ ] `src/index.js`: Hono アプリ、`fetch` / `scheduled` エクスポート
- [ ] `src/app.js`: ルート登録の器、`onError`、404
- [ ] `src/views/layout.js`: 共通HTML（ヘッダ・タブバー・CSS読み込み）
- [ ] `src/lib/datetime.js`: Asia/Tokyo 変換ヘルパ
- [ ] `src/db/queries.js`: D1 ラッパの土台
- [ ] `GET /healthz`（`SELECT 1`）
- [ ] ローカル起動確認、スキーマ `--local` 適用、`--remote` 適用
- [ ] 初回 `wrangler deploy`、`/healthz` 200 確認
- 完了条件: 本番URLが発行され `/healthz` が 200、全テーブル作成済み

### P1 認証・登録 ✅（2026-09-07 完了・コミット済）

- [x] `src/auth.js`: PBKDF2 ハッシュ、HMAC 署名 cookie、double-submit CSRF
- [x] `GET/POST /register`（SETUP_TOKEN 検証、最初の登録者を admin、上限4で締切）
- [x] `GET/POST /login`（試行制限 5回→15分ロック、`locked_until`）
- [x] `POST /logout`
- [x] 認証ミドルウェア（未ログイン→`/login?next=`）、admin ガード（`requireAuth`/`requireAdmin`）
- [x] レイアウトに現在ユーザー表示・ログアウト
- 完了条件: 登録→ログアウト→ログイン→保護ページ閲覧の一連が動く（ローカル/本番で確認済）

### P2 物件・テンプレ管理 ✅（2026-09-08 完了・コミット済）

- [x] `GET /admin`（各物件の最終同期サマリ。同期実行ボタンは P3）
- [x] 物件: 一覧（iCal URL マスク）/ 追加 / 編集（編集時は実 URL 表示）/ 無効化（`active=0`、物理削除なし）
- [x] テンプレ: 一覧 / 追加 / 複製 / 名称変更 / 削除（ベース id=1 と割当中は削除不可）
- [x] テンプレ項目: エリア・ラベル・写真必須・補足の追加/編集/削除、上下ボタンで並替（エリア内 sort_order 交換）
- [x] 物件へテンプレ割当（`property.template_id`。テンプレはフラット共有リスト、`checklist_template.property_id` は未使用）
- [x] ベーステンプレ「標準ベース」は seed 済（id=1・空）
- [x] ユーザー管理 `/admin/users`: 権限変更 / 有効化・無効化 / PIN リセット（6桁を1回表示）/ 登録受付の再開・停止
  - ガード: 最後の admin は降格・無効化不可 / 自分自身は無効化不可
- 実装: `src/admin.js`（Hono サブアプリ、`app.route("/admin", admin)`）、`src/views/admin.js`、`src/lib/ids.js`
- 完了条件: 物件1件＋テンプレ1件を画面から作成・編集できる（ローカルで全ルート確認済）

### P3 iCal 同期

- [ ] `src/ical/parser.js`: 折返し解除、プロパティ分解、VEVENT 抽出
- [ ] `src/ical/sync.js`: 取得→分類（予約/ブロック）→`reservation` upsert→消滅検知→`cleaning` 生成/更新→`sync_log`
- [ ] `scheduled()` から全 `active` 物件を同期
- [ ] `POST /admin/properties/:id/sync` / `POST /admin/sync-all`
- [ ] `/admin` に `sync_log` 表示
- [ ] **実データ検証**: 実際の Airbnb iCal を1物件登録し、SUMMARY/DESCRIPTION/DTEND の実値を確認、パーサ調整
- 完了条件: 実 iCal から予約が取り込まれ、チェックアウト日ごとに清掃が生成される

### P4 清掃一覧・詳細

- [ ] `GET /`（今後 / 直近、物件フィルタ、カード表示、進捗）
- [ ] `GET /cleanings/:id`（ヘッダ、状態、メモ）
- [ ] `POST /cleanings/:id/start` `/complete` `/reopen` `/note`
- [ ] `GET/POST /cleanings/new`（臨時清掃）
- [ ] 完了ガード（必須項目・必須写真の未了チェック）
- 完了条件: 清掃を開始→（チェック）→完了→再オープンできる

### P5 チェックリスト

- [ ] 清掃生成時に `checklist_template_item` → `checklist_item` スナップショット
- [ ] 詳細画面でエリア別グルーピング、行タップでトグル（`fetch` JSON）
- [ ] `checked_by`/`checked_at` 記録、エリア進捗・全体進捗
- [ ] `POST /cleanings/:id/items/:itemId/toggle`
- 完了条件: チェック操作が保存され、進捗が更新される

### P6 写真エビデンス

- [ ] `public/app.js`: `OffscreenCanvas` でフル(1600/≤1.5MB)・サムネ(400) 生成
- [ ] `POST /cleanings/:id/photos`（multipart、JPEG限定、サイズ検証、`photo`+`photo_blob`）
- [ ] `GET /photos/:id`（`?thumb=1` / `?view=1`、認証、`storage` 分岐、ETag/Cache）
- [ ] `POST /photos/:id/delete`（本人 or admin）
- [ ] 詳細画面: 清掃単位／項目単位の写真、サムネ一覧、拡大
- [ ] 必須写真の項目は写真なしでチェック不可 or 完了不可（要件 FR-17）
- 完了条件: 撮影→縮小→アップロード→サムネ表示→拡大→削除が動く

### P7 PWA

- [ ] `public/manifest.webmanifest`、アイコン（192/512/maskable、仮デザイン）
- [ ] `public/sw.js`（アプリシェル precache、network-first for `/`・API、SWR for `/photos/*`）
- [ ] レイアウトに `<link rel=manifest>` と iOS メタ、SW 登録
- [ ] iOS/Android 実機で「ホーム画面に追加」確認
- 完了条件: スタンドアロン起動、オフラインでアプリシェルが開く

### P8 仕上げ

- [ ] `GET /history`（物件・期間フィルタ、所要時間、写真、メモ）
- [ ] `/admin` に写真総容量・D1 使用量の目安、閾値警告
- [ ] エクスポート（CSV / JSON、任意）
- [ ] エラーページ、空状態、文言調整、スタイル微調整
- 完了条件: 履歴が遡れる、容量が見える

---

## 4. 依存関係

```
P0 ─▶ P1 ─▶ P2 ─▶ P3 ─▶ P4 ─▶ P5 ─▶ P6 ─▶ P7
                    │            └──────────┐
                    └─▶（実データ検証はP3で一度デプロイ）  P8（P4以降いつでも着手可）
```

- P5 は P2（テンプレ）と P4（清掃）に依存。
- P6 は P4/P5 に依存。
- P7/P8 は P6 完了後が望ましいが P4 以降で並行可。

---

## 5. リスクと対応

| リスク | 対応 |
|---|---|
| Airbnb iCal の形式が想定と違う | P3 で早期にデプロイし実データ確認。パーサをデータ駆動に |
| D1 容量（写真）が早く逼迫 | `/admin` で監視、閾値超で R2 移行（02 §7.4） |
| PIN の総当たり | 試行制限＋Secure cookie＋URL非公開（02 §6.2） |
| Workers 10ms CPU 制限 | 画像処理はクライアント側。サーバーは I/O 中心 |
| `assets` と動的ルートの競合 | 静的は `public/` 固定パス、動的ルートは接頭辞を分離 |

---

## 6. 進捗管理

- 本ファイルのチェックボックスを更新。
- フェーズ完了ごとに `docs/` の進捗表（01 §10）と本書を更新し、git コミット。
- コミットメッセージ接頭辞: `P0:` `P1:` ... 例 `P0: プロジェクト基盤とスキーマ`

---

## 変更履歴

| 日付 | 版 | 変更内容 |
|---|---|---|
| 2026-09-07 | 0.1 | 初版 |
